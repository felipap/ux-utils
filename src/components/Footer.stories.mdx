import { action } from '@storybook/addon-actions'
import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks'
import { SectionDivider } from './SectionDivider'

<Meta title='template/SectionDivider' component={SectionDivider} />

# SectionDivider

With `MDX` we can define a story for `SectionDivider` right in the middle of our
Markdown documentation.

<Canvas>
  <Story
    name='Regular'
    args={{
      label: 'This is a SectionDivider',
      onClick: action('clicked')
    }}
  >
    {args => <SectionDivider {...args} />}
  </Story>
</Canvas>
