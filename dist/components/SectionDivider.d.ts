/// <reference types="react" />
interface Props {
    from?: string;
    to?: string;
    flip?: boolean;
    straight?: boolean;
}
export declare function SectionDivider({ from, to, flip, straight }: Props): JSX.Element;
export {};
